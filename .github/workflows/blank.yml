name: Kali RDP

on:
  workflow_dispatch:

jobs:
  kali:
    runs-on: ubuntu-latest
    timeout-minutes: 3600

    steps:
      - name: Install Desktop & RDP
        run: |
          sudo apt update
          sudo apt install -y xfce4 xfce4-goodies xrdp dbus-x11
          sudo systemctl enable xrdp
          sudo adduser xrdp ssl-cert

      - name: Create user
        run: |
          sudo useradd -m kali
          echo "kali:kali" | sudo chpasswd
          sudo usermod -aG sudo kali

      - name: Install Tailscale
        run: |
          curl -fsSL https://tailscale.com/install.sh | sh

      - name: Connect Tailscale
        run: |
          sudo tailscale up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=kali-gh-${{ github.run_id }}

      - name: Install Kali tools (safe)
        run: |
          sudo apt install -y \
            nmap net-tools dnsutils hydra john wireshark \
            metasploit-framework armitage exploitdb sqlmap \
            nikto gobuster responder smbclient enum4linux

      - name: Configure XFCE
        run: |
          echo "xfce4-session" | sudo tee /home/kali/.xsession
          sudo chown kali:kali /home/kali/.xsession

      - name: Show login info
        run: |
          echo "======================"
          echo "RDP LOGIN"
          echo "User: kali"
          echo "Pass: kali"
          echo "Tailscale IP:"
          tailscale ip -4
          echo "======================"

      - name: Keep alive
        run: |
          while true; do sleep 300; done
